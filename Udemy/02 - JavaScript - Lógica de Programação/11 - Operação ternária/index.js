// montado uma operação ternária
// (condição) ? 'valor para verdadeiro' : 'valor para falso';
const pontuacaoUsuario = 1500;
const nivelUsuario = pontuacaoUsuario >=1000 ? 'usuario VIP' : 'usuario normal';
console.log(nivelUsuario);